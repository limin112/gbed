{
  "name": "gdrive-image-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local proxy server for uploading images to Google Drive via gws",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  }
}
